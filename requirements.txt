httpx>=0.27          # HTTP client; bundles certifi so TLS works out of the box
python-dotenv>=1.0   # load tunables from .env
tenacity>=8.2        # retry with exponential backoff on transient HTTP errors
duckdb>=1.1
pandas>=2.0
